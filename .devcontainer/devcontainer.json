// For format details, see https://aka.ms/devcontainer.json. For config options, see the
// README at: https://github.com/devcontainers/templates/tree/main/src/debian
{
  "name": "outline-web.devcnt",
  // Or use a Dockerfile or Docker Compose file. More info: https://containers.dev/guide/dockerfile
  "image": "mcr.microsoft.com/devcontainers/base:bookworm",
  "features": {
    "ghcr.io/devcontainers/features/go:1": {},
    "ghcr.io/devcontainers/features/node:1": {}
  },
  "workspaceMount": "source=${localWorkspaceFolder},target=/workspace,type=bind",
  "workspaceFolder": "/workspace",
  // Features to add to the dev container. More info: https://containers.dev/features.
  // "features": {},
  // Use 'forwardPorts' to make a list of ports inside the container available locally.
  // "forwardPorts": [],
  "mounts": [
    "type=volume,source=outline-web.devcnt-node_modules,target=/workspace/node_modules",
    "type=volume,source=outline-web.devcnt-bin,target=/workspace/bin",
    "type=volume,source=outline-web.devcnt-dist,target=/workspace/static/dist"
  ],
  "postCreateCommand": "sudo chmod 777 node_modules && sudo chmod 777 bin && sudo chmod 777 static/dist",
  // Configure tool-specific properties.
  // "customizations": {},
  // Uncomment to connect as root instead. More info: https://aka.ms/dev-containers-non-root.
  // "remoteUser": "root"
  "runArgs": [
    "--name",
    "outline-web.devcnt"
  ]
}